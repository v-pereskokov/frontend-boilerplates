const initClientConfig = require('./initClientConfig');
const initServerConfig = require('./initServerConfig');
const loadAssets = require('./loadAssets');
const loadScripts = require('./loadScripts');
const loadStyles = require('./loadStyles');
const loadNullStyles = require('./loadNullStyles');

module.exports = {
    initClientConfig,
    initServerConfig,
    loadAssets,
    loadScripts,
    loadStyles,
    loadNullStyles,
};
